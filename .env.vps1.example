# ══════════════════════════════════════════════════════════════
# .env.vps1.example — VPS1 "Hậu cung" (GĐ2)
# ══════════════════════════════════════════════════════════════
# Chạy: postgres, redis, emqx, iot-gateway, worker-service
# DB/Redis chạy local (service name), expose qua Tailscale cho VPS2
#
# ⚠️ Copy nội dung này vào GitHub Secret: VPS1_ENV
# ⚠️ THAY TẤT CẢ GIÁ TRỊ "change-me-*" TRƯỚC KHI DÙNG

# ──────────────────────────────────────────────
# APPLICATION
# ──────────────────────────────────────────────
APP_ENV="production"
APP_NAME="aurathink"
APP_DEBUG=false
APP_LOG_LEVEL="warn"

# ──────────────────────────────────────────────
# HTTP SERVER
# ──────────────────────────────────────────────
HTTP_HOST="0.0.0.0"
HTTP_PORT=3001
HTTP_VERSIONING_ENABLE=true
HTTP_VERSION=1
APP_CORS_ORIGINS="https://aurathink.ddns.net"

# ──────────────────────────────────────────────
# MICROSERVICE PORTS
# ──────────────────────────────────────────────
SOCKET_GATEWAY_PORT=3002
IOT_GATEWAY_PORT=3003
WORKER_SERVICE_PORT=3004

# ──────────────────────────────────────────────
# AUTHENTICATION & JWT
# ──────────────────────────────────────────────
AUTH_ACCESS_TOKEN_SECRET="change-me-use-openssl-rand-base64-32"
AUTH_REFRESH_TOKEN_SECRET="change-me-use-openssl-rand-base64-32"
AUTH_ACCESS_TOKEN_EXP="1d"
AUTH_REFRESH_TOKEN_EXP="7d"

# ──────────────────────────────────────────────
# DATABASE — LOCAL (postgres service name)
# ──────────────────────────────────────────────
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me-strong-db-password
POSTGRES_DB=aurathink
DATABASE_URL="postgresql://postgres:change-me-strong-db-password@postgres:5432/aurathink?schema=public"

# ──────────────────────────────────────────────
# REDIS — LOCAL (redis service name)
# ──────────────────────────────────────────────
REDIS_HOST="redis"
REDIS_PORT=6379
REDIS_USERNAME=default
REDIS_PASSWORD="change-me-redis-password"
REDIS_ENABLE_TLS=false

# ──────────────────────────────────────────────
# MQTT (EMQX) — LOCAL (emqx service name)
# ──────────────────────────────────────────────
MQTT_HOST=mqtt://emqx
MQTT_PORT=1883
MQTT_USER=smarthome-server
MQTT_PASS=change-me-mqtt-password

# ──────────────────────────────────────────────
# EMQX — Node name = Tailscale IP của VPS1
# ──────────────────────────────────────────────
EMQX_NODE_NAME=emqx@100.117.220.15
EMQX_DASHBOARD_USER=admin
EMQX_DASHBOARD_PASS=change-me-dashboard-password

# ──────────────────────────────────────────────
# TAILSCALE — Bind postgres/redis port cho VPS2 kết nối
# ──────────────────────────────────────────────
TAILSCALE_IP=100.117.220.15

# ──────────────────────────────────────────────
# MAIL (SMTP)
# ──────────────────────────────────────────────
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_FROM="Smart Home Support <your-email@gmail.com>"

# ──────────────────────────────────────────────
# SSL & MISC
# ──────────────────────────────────────────────
SSL_CERT_PATH=./deploy/docker/ssl
SENTRY_DSN=""
ADMIN_EMAIL=admin@your-domain.com
ADMIN_PASSWORD=change-me-admin-password
APP_VERSION=latest
